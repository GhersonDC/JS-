const parent = document.getElementById('parent');

// console.log(parent.childNodes) //devuelve todos los nodos del parent
// console.log(parent.children)   //devuelve solo los elementos del parent

// console.log(parent.parentNode) //devulve el tipo de nodo padre del padre
// console.log(parent.parentElement) //devulve el tipo de nodo padre del padre

// console.log(parent.firstElementChild) //devuelve el primer nodo hijo

// console.log(parent.hasChildNodes()) //devuelve booleano donde devuelve true si contiene un nodo hijo dentro

console.log(parent.nextElementSibling) //localizamos un elemento sibling del parent


